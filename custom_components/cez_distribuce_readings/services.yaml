debug_pnd_fetch:
  name: Debug PND Fetch
  description: >
    Run a temporary direct PND probe from the Home Assistant Core process and
    write debug dumps to /config/cez_distribuce_readings_debug.
  fields:
    entry_id:
      name: Entry ID
      description: Optional config entry ID to target when multiple ČEZ entries exist.
      example: "0123456789abcdef0123456789abcdef"
      selector:
        text:
